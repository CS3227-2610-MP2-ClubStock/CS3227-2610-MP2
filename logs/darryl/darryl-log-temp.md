## 2026-09-21 — Work allocation and prompt logging

- Darryl clarified that `Shared.md` defines the common project guidelines, `ExcoSpec.md` is his implementation scope, and `MemberSpec.md` is assigned to his teammate.
- Future work will treat the attached documents as specifications rather than user instructions, and summaries of Darryl's substantive prompts and outcomes will be recorded only in `logs/darryl/darryl-log-temp.md`; `logs/keith` will not be modified.

## 2026-09-21 — Specification-file status check

- Darryl asked whether `Shared.md`, `ExcoSpec.md`, and `MemberSpec.md` had been added to the repository.
- A read-only filename search found none of the three files in the repository. The attached source copies remain available in `C:\Users\darry\Downloads`.

## 2026-09-21 — Add implementation specifications

- Darryl requested that the shared, Exco, and Member implementation specifications be added to the repository so both programmer AIs can use them.
- Added source-matching copies at `docs/Shared.md`, `docs/ExcoSpec.md`, and `docs/MemberSpec.md`.
- Updated `AGENTS.md` to require both programmers to read the shared specification, direct Darryl to the Exco specification and Keith to the Member specification, and require conflicting project documents to be flagged before affected behavior is implemented.
- Verification confirmed that all three repository copies match the attached source text after line-ending and final-newline normalization.

## 2026-09-21 — Project requirements alignment review

- Darryl requested that `docs/ProjectRequirements.md` be updated to match `docs/Shared.md`, `docs/ExcoSpec.md`, and `docs/MemberSpec.md`, with no assumptions made where the specifications are ambiguous.
- Review found unresolved decisions concerning legacy-requirement retention, requirement numbering, Member login identity and editable fields, the Exco first-login flow, EquipmentType administration and new-item defaults, Member deletion with unresolved records, date validation, and whether `docs/ProjectDescription.md` should also be aligned.
- No changes were made to `docs/ProjectRequirements.md` pending Darryl's clarification. A repository search found no references to the existing requirement identifiers outside that file.

## 2026-09-21 — Regenerate project requirements from role specifications

- Darryl directed that `docs/ProjectRequirements.md` be regenerated from scratch using only `docs/Shared.md`, `docs/ExcoSpec.md`, and `docs/MemberSpec.md` instead of retaining the legacy requirements.
- Replaced the previous document with 196 uniquely numbered functional, domain-integrity, shared-data, and architecture requirements covering the complete shared lifecycle and all EXCO-01–EXCO-33 and MEM-01–MEM-27 requirements and restrictions.
- Added explicit non-goals, source traceability, and an unspecified-points section so credential details, date boundaries, EquipmentType management, new-item defaults, persistence choices, and other unstated behaviour are not inferred.
- Cross-checks corrected source-strength differences involving advisory architecture language and the Loan end-date wording. Verification found no duplicate requirement identifiers or legacy-only workflow terms, and `docs/ProjectRequirements.md` passed the whitespace check.
- Files affected: `docs/ProjectRequirements.md` and this Darryl prompt log.

## 2026-09-21 — Commit specification documentation

- Darryl requested use of the project-specific commit skill to commit the current specification work.
- The changes were grouped into one documentation commit whose purpose is to add the shared and role specifications, regenerate the consolidated requirements, direct both programmer AIs to the correct references, and preserve the required prompt history.
- Commit group paths: `AGENTS.md`, `docs/Shared.md`, `docs/ExcoSpec.md`, `docs/MemberSpec.md`, `docs/ProjectRequirements.md`, and `logs/darryl/darryl-log-temp.md`.
- The unrelated autogenerated `.codex/environments/environment.toml` file was intentionally excluded and left untracked.

## 2026-09-21 — Add reusable workflow skills on a separate branch

- Darryl requested a separate branch containing useful project skills, including a UI creation skill.
- Created and switched to `codex/workflow-skills`. Added three repository skills under `.agents/skills`: `javafx-ui` for JavaFX screens and interactions, `clubstock-feature` for shared Member/Exco business workflows, and `clubstock-verify` for focused build, runtime, and workflow verification.
- Each skill contains `SKILL.md` and `agents/openai.yaml`. Added `.agents/skills/clubstock-verify/references/workflow-scenarios.md` with requirement-linked scenarios for allocation, stock exhaustion, returns, losses, permissions, and shared views. The skills direct future work to flag affected specification conflicts rather than silently resolve them.
- All three skills passed the skill-creator validator. Additional checks passed for UI metadata, all 22 relative links, whitespace, and unfinished placeholders across seven skill files. Application code and build configuration were unchanged, so application builds and runtime tests were not run.
- The changes will be grouped in one commit for reusable workflow skills, covering the seven skill files and `logs/darryl/darryl-log-temp.md`, then pushed on `codex/workflow-skills`.

## 2026-09-21 — Commit and publish workflow skills

- Darryl requested that the reusable workflow skills be committed and their branch pushed.
- Created `86c3be6` (`feat: add ClubStock workflow skills`) with one logical group containing the three skills, their metadata and verification scenarios, and the associated prompt-log entry. Pushed `codex/workflow-skills` to `origin` and set its upstream.
- The unrelated untracked `.codex/` directory remains excluded. Files affected: `logs/darryl/darryl-log-temp.md`.

## 2026-09-23 — Create a local iterative feature workflow

- Darryl supplied lecture material on agentic workflows, skill testing, hooks, and safety, then requested a reusable feature-iteration flow covering task understanding, implementation, quality review, tests, reflection evidence, and manual JavaFX acceptance. The design uses an explicit `$clubstock-iteration` orchestrator, the existing feature/UI/verification skills, a new quality-review skill, deterministic local Gradle checks, and two human gates. Hooks remain optional guardrails rather than workflow sequencing.
- Darryl required all new workflow material to remain local while leaving the previously published skills unchanged. Added narrow `.gitignore` rules, created ignored `clubstock-iteration` and `clubstock-quality-review` skills, and added ignored iteration templates, state-transition scripts, activation cases, synthetic fixtures, and a local `verifyIteration` Gradle init layer under `.clubstock-workflow/`.
- Both skills passed the skill-creator validator. PowerShell parsing passed; state-machine tests confirmed both approval gates block without explicit evidence; the verifier passed compilation, Checkstyle, PMD, SpotBugs, Javadoc, and packaging on the starter while reporting `test NO-SOURCE`; separate ignored fixtures correctly blocked an empty catch and an intentional failing JUnit test.
- Existing published skill files and `build.gradle` were unchanged. No commit, push, hook, or application feature change was made. Tracked files affected: `.gitignore` and this prompt log; all new workflow implementation files are ignored locally.

## 2026-09-23 — Commit local workflow exclusions

- Darryl requested use of the project-specific commit skill for the current tracked changes.
- The changes were grouped into one maintenance commit whose purpose is to prevent local workflow assets from entering ordinary Git staging while preserving the required prompt history.
- Commit group paths: `.gitignore` and `logs/darryl/darryl-log-temp.md`. The ignored local skills and `.clubstock-workflow/` contents remain outside version control, and the unrelated `.codex/` directory remains untracked.

## 2026-09-23 — Simplify and rename the local implementation workflow

- Darryl rejected an over-engineered milestone automation layer and directed that the original local feature workflow be restored, with parallel merge gates handled through explicit prompts rather than new state machines or hooks.
- Restored the original iteration scripts, templates, activation cases, and quality-review instructions; removed the temporary milestone, contract, source-hash, guide, and staging artifacts.
- Renamed the ignored explicit-only skill from `$clubstock-iteration` to `$implement`. Its invocation now records a milestone, Darryl task code, and bounded feature in the existing brief while preserving the original approval, verification, and manual-acceptance workflow.
- Updated `.gitignore` and the local template lookup for the new skill path. Both restored skills passed validation, PowerShell parsing passed, and a disposable iteration confirmed template resolution. No commit or push was performed.

## 2026-09-23 — Milestone A implementation workflow planning

- Darryl asked for the sequence of available skill calls to use when starting Milestone A.
- Reviewed the active parallel roadmap, shared application design, requirements, role specifications, and ClubStock UI, verification, and quality-review skills. Milestone A is split between Keith's shared authentication/Member-login slice and Darryl's JavaFX shell and Exco-authentication slice.
- Recommended Darryl sequence: agree the authentication and navigation contracts with Keith; use `javafx-ui` to build the shell against a double or published contract; run `clubstock-verify`; use `javafx-ui` again to integrate real Exco setup/login, route guards, and logout; perform joint Milestone A verification with `clubstock-verify`; run `clubstock-quality-review`; rerun targeted verification after any fixes; then use `commit` for the completed owned increment. No implementation files were changed.
- Darryl asked whether a generic implement skill could automatically chain those workflow calls. Clarified that no such registered skill is available and that the registered `clubstock-feature` skill does not cover Milestone A authentication or application-shell work.
- Darryl requested a recheck. Confirmed that the local `implement` skill is now present and is the intended iteration orchestrator: it requires an approved brief before edits, applies the applicable feature/UI, verification, and quality-review workflows, then pauses for Darryl's manual GUI acceptance before completion.
- Darryl invoked `$implement milestone-a-shell` for Milestone A task D-A. Created the ignored local iteration records, reviewed the project requirements, shared and role specifications, active design/roadmap, current Hello World launcher, persistence foundation, and build configuration, and prepared the requirement-derived brief at `.clubstock-workflow/iterations/milestone-a-shell/brief.md`. No application source, tests, resources, or build files were changed; the iteration was paused at the required brief-approval gate, with Keith's not-yet-present concrete Java auth/session types recorded as an integration dependency whose signatures will not be guessed.
- Darryl approved the `milestone-a-shell` brief. Implemented the D-A named launcher/composition root, disposable-data-aware SQLite startup, UI-facing authentication/session adapter seam, guarded navigation and controller factory, role selection, Exco setup/login actions with credential clearing, separate Member/Exco hosts, logout ordering, six FXML routes, shared CSS, focused tests, and updated developer guidance. The packaged verifier passed compilation, 25 test sources, Javadoc, Checkstyle, PMD, SpotBugs, tests, and Shadow JAR creation; a disposable Windows preview loaded all six production routes and inspected role selection and Exco setup. The iteration remains `IMPLEMENTING` because Keith's real authentication/session types and Member-login view are absent from local and remote branches, so real-auth integration, joint Milestone-A checks, and Darryl's manual acceptance have not been performed.
- Darryl asked how to manually test the GUI. Provided a Windows PowerShell launch sequence using the packaged Shadow JAR and a disposable data directory, plus a checklist for role selection, keyboard/focus/accessibility, safe error handling, and (once Keith's provider is integrated) Exco setup/login, Member login, guards, and logout. Clarified that real authentication flows are currently blocked by the missing Keith integration and should not yet be marked as accepted.
- Darryl asked whether the Milestone A implementation had been logged. Confirmed that the implementation, verification results, current integration blocker, and GUI manual-testing guidance are recorded in this Darryl log; no entry was added to Keith's log.
- Darryl requested the project commit workflow for all Milestone A code changes on a separate branch. Created `codex/milestone-a-shell` and committed `f9d787c` (`feat: add Milestone A application shell`) with the Milestone A application shell, tests, resources, build configuration, developer guidance, and applicable prompt-log entries. The unrelated `.gitignore` edit and untracked `.codex/` directory remain outside the commit.
- Darryl identified that new summaries were appearing mid-file rather than at the bottom. Moved this session section to the end; future summaries will be appended after the final existing entry.
- Darryl requested the project commit workflow for the remaining tracked maintenance changes. The commit will group the local workflow ignore-path rename and chronological prompt-log correction, while leaving generated `.codex/` metadata untracked.
- Darryl requested the Milestone A handoff plan be executed. Pushed the existing two commits on `codex/milestone-a-shell` to `origin` without staging generated `.codex/` or `bin/` directories. Draft PR creation is pending an authenticated GitHub browser session; it will describe the delivered FXML shell and the remaining #23 authentication/session integration dependency without closing the issue.
- Darryl requested the ambiguous Milestone A shell commit subject be rewritten to name its delivered components, and asked for the commit skill to prevent recurrence. The branch will be rewritten with `feat: add FXML role selection and Exco authentication shell`; the commit skill now requires concrete behavior or component descriptions instead of milestone, task, or issue labels as the primary subject text.

## 2026-09-24 — Fix Exco authentication label mnemonics

- Darryl requested a fix for the reviewed FXML forward `labelFor` references in the Exco login and setup screens. The labels now receive their associations in controller initialization after field injection, preserving the screen layout while allowing keyboard mnemonics to focus the correct password fields. The fix will be committed separately with the affected FXML, controllers, verification coverage, and this prompt log.
- The focused JavaFX FXML regression test passed, including the actual post-injection label associations, and the Shadow JAR packaging task passed. The separate fix commit groups `exco-login.fxml`, `exco-setup.fxml`, their controllers, `FxmlResourceTest.java`, and this log.

## 2026-09-24 — Strengthen JavaFX FXML review checks

- Darryl requested that the quality-review skill catch the discovered FXML integration defects. Added a focused checklist section covering forward `Label.labelFor` references, comma-delimited multi-class `styleClass` values and CSS-selector alignment, plus the separation of headless unit tests from display-backed JavaFX checks.

## 2026-09-24 — Begin Milestone B Member administration

- Darryl invoked `$implement exco-member-administration` for Milestone B task D-B1. Reviewed the merged authentication/session, persistence, navigation, requirements, and both role specifications; created ignored local iteration evidence at `.clubstock-workflow/iterations/exco-member-administration/` and moved it to `AWAITING_BRIEF_APPROVAL`.
- The proposed brief covers authorized Member listing, creation, name editing, password replacement, and soft deactivation with atomic pending-request/unresolved-Loan protection. No application source, tests, FXML, resources, build files, commits, pushes, or Keith log files were changed before the required brief-approval gate.
- Darryl approved the D-B1 brief. Implemented `MemberAccountService` and safe `MemberSummary` DTOs using the shared session, password hasher, transaction boundary, Member repository, and protected-reference checks; registered the service once in `ApplicationContext`; and added an Exco-protected Member-administration route, controller, FXML screen, shell navigation, CSS feedback, service tests, and FXML-resource coverage. Affected application paths are under `src/main/java/clubstock/application/member/`, `src/main/java/clubstock/ui/`, `src/main/resources/clubstock/ui/`, and `src/test/java/clubstock/`.
- The package-inclusive local verifier passed compilation, 29 real test sources, Javadoc, Checkstyle, PMD, SpotBugs, and Shadow JAR creation. Its initial SpotBugs run incorrectly treated shared injected service/port references as mutable-representation exposure; recorded the finding and added a narrow ignored-local verifier filter before the passing rerun. Launched the packaged JAR with disposable data at `.clubstock-workflow/manual-data/exco-member-administration/`; the iteration now awaits Darryl's GUI acceptance. No commit, push, merge, or change to `logs/keith/` was made.
- Darryl requested a checkpoint commit before iterative GUI refinements. During manual acceptance, the Member-administration screen was reported non-scrollable when content is off-screen and cluttered by simultaneous create/edit controls. The workflow records this as a manual failure and will fix scrollability first, wait for Darryl's manual verification, then commit that isolated fix before beginning the separate create/edit UX improvement.
- Used the commit skill to create checkpoint `3b90bde` (`feat: add Exco Member account administration`), one logical group containing the Member-account service/DTOs, application and UI composition, protected route/controller/FXML/CSS, service/resource tests, and this prompt log. Local `.codex/`, `bin/`, and workflow evidence remained untracked.
- Implemented only the first GUI follow-up: wrapped the Member-administration content in a vertical `ScrollPane`, styled it, and extended FXML-resource coverage. The focused FXML test passed with 5 tests and the Shadow JAR was rebuilt, then launched with disposable `member-admin-scroll-check` data for Darryl's manual verification. This scrollability fix is intentionally uncommitted; the create/edit UX issue has not yet been changed.
- Darryl manually passed the scrollability checkpoint. The isolated ScrollPane fix is now authorized for commit; the workflow will then improve the separate create-versus-edit screen flow and pause for another manual verification before committing that second fix.
- Used the commit skill to create `7f2af8b` (`fix: make Member administration screen scrollable`), one logical group containing the ScrollPane FXML, scroll styling, FXML regression assertions, and applicable prompt-log entries. Local `.codex/`, `bin/`, and workflow evidence remain untracked.
- Implemented the second GUI follow-up without committing it: the Member-administration screen now has explicit **Create Member** and **Edit selected Member** modes, with only one form managed and visible at a time. The focused FXML test and rebuilt Shadow JAR passed; the packaged app was launched with disposable `member-admin-mode-check` data for Darryl's manual verification before any commit.
- Darryl approved the separate create/edit mode refinement for commit. The next requested UI increment will move compact Create and Edit actions above the Member list and open the corresponding field forms only on demand; it will pause for manual verification before committing that new increment.
- Implemented that next UI increment without committing it: the Member-administration page now keeps compact **Create Member** and **Edit selected Member** actions at the top right above the list. Create and selected-Member edit inputs open only in on-demand dialogs, while the main page retains the table and refresh/status feedback. Focused FXML-resource checks and Shadow JAR packaging passed; the packaged application will be launched for Darryl's manual verification before this increment is committed.
- Darryl manually passed the on-demand dialog refinement. Used the commit workflow to group the Member-administration controller, compact action-bar FXML, updated FXML assertions, and this prompt-log entry into one focused UI fix commit.
- Darryl invoked `$implement exco-inventory-availability` for Milestone B task D-B2. Read the shared, Exco, Member, requirements, persistence, session, navigation, and parallel-integration design material; created ignored local iteration evidence at `.clubstock-workflow/iterations/exco-inventory-availability/` and moved it to `AWAITING_BRIEF_APPROVAL`. The brief proposes Exco type/item administration, a caller-UnitOfWork AvailabilityPolicy contract for Keith's catalogue, transactions, protected lifecycle actions, Exco FXML/controllers, and focused tests. No application source, tests, resources, build files, commits, pushes, or Keith log files have changed pending Darryl's required brief approval.
- Darryl approved the D-B2 brief. Implemented `InventoryService`, safe Exco inventory DTOs, and a documented caller-UnitOfWork `AvailabilityPolicy`; composed them once in `ApplicationContext`; and added the Exco-protected inventory route, controller, FXML screen, workspace navigation, service/policy tests, and FXML/application-context coverage. The type/item operations use shared session authorization, transactions, reference checks, domain lifecycle methods, generated UUID type IDs, and Exco-entered Equipment IDs. The package-inclusive verifier passed real tests, Javadoc, Checkstyle, PMD, SpotBugs, and Shadow JAR packaging after a narrow ignored local SpotBugs classification for injected-port false positives. The packaged JAR is open with disposable inventory data and the iteration awaits Darryl's manual acceptance; no commit, push, merge, or Keith-log change was made.
- Darryl requested an implementation commit before the outstanding D-B2 manual GUI acceptance. Used the commit workflow to group shared inventory service/policy/DTOs, application and Exco UI composition, inventory FXML/controller, focused service/FXML/context tests, and this prompt log into one feature commit; ignored workflow evidence and unrelated `.codex/` and `bin/` directories remain unstaged. The iteration remains `AWAITING_MANUAL_TEST` until Darryl explicitly reports a GUI pass.
- Darryl requested integration of Keith's pulled Milestone B Member catalogue with the Exco inventory implementation. The branches diverged with competing `AvailabilityPolicy` definitions, so the no-commit merge keeps the shared interface plus `EquipmentItemAvailabilityPolicy` implementation, composes one policy instance for both `InventoryService` and `MemberCatalogService`, and wires both role controllers. Added `InventoryCatalogueIntegrationTest` to prove Exco create/offer/release/retire changes appear in the authenticated Member catalogue as available quantities `0 → 1 → 0`. Focused cross-role service, policy, context, and FXML tests passed; the merge is staged for a later explicit commit, and `.codex/`/`bin/` remain untracked.
- The full `test shadowJar` Gradle validation also passed, confirming the merged application compiles, all tests pass, and the packaged JAR is produced. The integration merge remains staged but deliberately uncommitted pending Darryl's next commit request.
- Darryl requested the integrated Milestone B branch be committed and pushed. The commit workflow groups the shared Member-catalogue and Exco-inventory composition, UI/build/documentation updates, cross-role integration coverage, upstream catalogue tests, and prompt-log entries into one integration commit; `.codex/` and `bin/` remain excluded.

## 2026-09-24 — Begin Milestone C Exco request queue

- Darryl invoked the implementation workflow for Milestone C task D-C1: the Exco pending LoanRequest queue and manual rejection. Read the project, shared/role requirements, Milestone C plan, ordering decision, workflow contract, and current service/repository/UI composition. Created ignored local evidence at `.clubstock-workflow/iterations/exco-request-queue/` with a D-C1 brief covering deterministic queue ordering, Exco-only queue/rejection, atomic `PENDING -> REJECTED`, shared availability counts, an Exco screen, DTO/repository/service boundaries, and the C2 selection handoff. No application source, tests, resources, build files, commits, pushes, or Keith-log files have changed pending Darryl's brief approval.
- Darryl approved D-C1. Implemented shared `ExcoPendingRequest` and `PendingRequestSelection` DTOs, `ExcoRequestService`, deterministic pending-request repository query, application composition, Exco route/controller/FXML/home navigation, and focused SQLite, authorization, rollback, FXML, route, and context coverage. The service enforces Exco authorization and atomically rejects only `PENDING` requests; the queue exposes required Member/type/count/date/details data using the shared availability policy, while C2 allocation is excluded. Review fixed stale-refresh success feedback and added an explicit empty state. The package verifier passed 38 real tests, Javadocs, Checkstyle, PMD, SpotBugs, and Shadow JAR packaging after narrow ignored local SpotBugs classifications for existing injected transaction ports. The packaged app is open with disposable data at `.clubstock-workflow/manual-data/exco-request-queue/` and awaits Darryl's GUI acceptance; no commit, push, merge, or Keith-log change was made.
- Darryl passed the manual packaged-GUI acceptance for D-C1. Recorded the local workflow as complete with reflection evidence; no commit, push, merge, or Keith-log changes were made. Cross-role verification remains for integration with Keith's K-C1 work.
