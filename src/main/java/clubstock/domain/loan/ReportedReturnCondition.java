package clubstock.domain.loan;

/**
 * Represents the condition reported by a Member when returning an item.
 */
public enum ReportedReturnCondition {
    /** Member reports that the item appears to be in good condition. */
    GOOD,
    /** Member reports that the item appears to be damaged. */
    DAMAGED
}
